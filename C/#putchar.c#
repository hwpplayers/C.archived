/*#include <stdio.h>
#include <conio.h>

int main(void)
{
	char ch;

	while ((ch = getch()) != 'q')
		putchar(ch);

	return 0;
}*/
/*https://askubuntu.com/questions/529114/why-cant-i-use-conio-h-or-ncurses-h*/

#include <stdio.h>
#include <ncurses.h>

int main(void)
{
  char ch;

  while ((ch = getch()) != 'q')
  while ((ch = getch()) != 'q')  
		putchar(ch);

	return 0;
}
